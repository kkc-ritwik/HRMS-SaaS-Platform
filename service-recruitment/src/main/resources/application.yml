server:
  port: 8085
spring:
  application:
    name: service-recruitment
  datasource:
    url: jdbc:postgresql://localhost:5433/hrms_db
    username: hrms_user
    password: hrms_pass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    table: flyway_schema_history_recruitment
    baseline-on-migrate: true
    baseline-version: 0
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: service-recruitment-group
      auto-offset-reset: earliest
  data:
    redis:
      host: localhost
      port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
jwt:
  secret: hrms-platform-jwt-secret-key-change-this-in-production-256bit!!
logging:
  level:
    com.hrms: DEBUG
