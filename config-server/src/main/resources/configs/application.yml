spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/hrms_db
    username: hrms_user
    password: hrms_pass
  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
jwt:
  secret: hrms-platform-jwt-secret-key-change-in-prod-256bits!!
  access-token-expiry: 3600000
  refresh-token-expiry: 604800000
